<Page
    x:Class="BulkRenamer.HistoryPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BulkRenamer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <StackPanel Grid.Row="0" Margin="32,32,32,0">
            <Grid>
                <Grid.ColumnDefinitions>
                     <ColumnDefinition Width="*" />
                     <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <TextBlock x:Uid="HistoryPageTitle" Text="History" Style="{StaticResource TitleTextBlockStyle}" Margin="0,0,0,12" />
                <Button Grid.Column="1" x:Name="ClearHistoryButton" x:Uid="ClearHistoryButton" Content="Clear History" Click="OnClearHistory" Visibility="Collapsed" />
            </Grid>
             <InfoBar x:Name="StatusBar" IsOpen="False" Severity="Informational" Margin="0,0,0,12" />
        </StackPanel>

        <ScrollViewer Grid.Row="1" VerticalScrollBarVisibility="Auto" Padding="15.5,0,20,32">
            <StackPanel Spacing="20">
                 <ListView x:Name="HistoryList" ItemsSource="{x:Bind History}" SelectionMode="None">
                    <ListView.ItemTemplate>
                        <DataTemplate x:DataType="local:HistoryEntry">
                            <Grid Padding="12" Margin="0,4" Background="{ThemeResource LayerFillColorDefaultBrush}" CornerRadius="8" BorderThickness="1" BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}">
                                <Grid.ColumnDefinitions>
                                    <ColumnDefinition Width="*" />
                                    <ColumnDefinition Width="Auto" />
                                </Grid.ColumnDefinitions>
                                
                                <StackPanel Spacing="4">
                                    <StackPanel Orientation="Horizontal" Spacing="8">
                                        <TextBlock Text="{x:Bind Summary}" Style="{StaticResource BodyStrongTextBlockStyle}" />
                                        <Border Background="{x:Bind StatusColor, Mode=OneWay}" CornerRadius="4" Padding="6,2">
                                            <TextBlock Text="{x:Bind StatusText, Mode=OneWay}" FontSize="10" Foreground="White" FontWeight="Bold" />
                                        </Border>
                                    </StackPanel>
                                    <TextBlock Text="{x:Bind SettingsSummary}" Style="{StaticResource CaptionTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}" TextWrapping="Wrap" />
                                    <Expander x:Uid="HistoryFilesExpander" IsExpanded="False" HorizontalAlignment="Left" Margin="0,6,0,0" Width="420" MaxWidth="500">
                                        <ItemsControl ItemsSource="{x:Bind Records}" HorizontalAlignment="Left">
                                            <ItemsControl.ItemTemplate>
                                                <DataTemplate x:DataType="local:FileHistoryRecord">
                                                    <TextBlock Text="{x:Bind OriginalPath}" Style="{StaticResource CaptionTextBlockStyle}" TextWrapping="Wrap" />
                                                </DataTemplate>
                                            </ItemsControl.ItemTemplate>
                                        </ItemsControl>
                                    </Expander>
                                </StackPanel>
                                
                                <StackPanel Grid.Column="1" Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                                    <Button Background="Transparent" Click="OnRestoreSettings" Tag="{x:Bind Settings}" ToolTipService.ToolTip="Reuse Settings" x:Uid="ReuseButton">
                                        <FontIcon Glyph="&#xE70F;" FontSize="14"/>
                                    </Button>
                                    
                                    <Button Click="OnUndoBatch" Tag="{x:Bind}" Visibility="{x:Bind CanUndo, Mode=OneWay}" Style="{StaticResource AccentButtonStyle}" x:Uid="UndoButton">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE7A7;" FontSize="14"/>
                                            <TextBlock x:Uid="UndoButtonText" Text="Undo" />
                                        </StackPanel>
                                    </Button>

                                    <Button Click="OnRedoBatch" Tag="{x:Bind}" Visibility="{x:Bind CanRedo, Mode=OneWay}" Style="{StaticResource AccentButtonStyle}" x:Uid="RedoButton">
                                        <StackPanel Orientation="Horizontal" Spacing="8">
                                            <FontIcon Glyph="&#xE7A6;" FontSize="14"/>
                                            <TextBlock x:Uid="RedoButtonText" Text="Redo" />
                                        </StackPanel>
                                    </Button>
                                </StackPanel>
                            </Grid>
                        </DataTemplate>
                    </ListView.ItemTemplate>
                 </ListView>
                 
                 <StackPanel x:Name="EmptyState" Visibility="Collapsed" HorizontalAlignment="Center" Spacing="16" Margin="0,60,0,0">
                    <FontIcon Glyph="&#xE81C;" FontSize="48" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Uid="NoHistoryTitle" Text="No history yet." Style="{StaticResource SubtitleTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                    <TextBlock x:Uid="NoHistoryDescription" Text="Your renaming actions will appear here." Style="{StaticResource BodyTextBlockStyle}" Foreground="{ThemeResource TextFillColorSecondaryBrush}"/>
                 </StackPanel>
            </StackPanel>
        </ScrollViewer>
    </Grid>
</Page>
