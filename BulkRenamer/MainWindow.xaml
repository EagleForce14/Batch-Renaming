<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BulkRenamer.MainWindow"
    x:Name="RootWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BulkRenamer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Batch Renaming">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBarHost" Height="32" Padding="8,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Image Source="Assets/Square44x44Logo.scale-400.png" Width="22" Height="22" Stretch="Uniform" />
                <TextBlock x:Uid="AppTitle" VerticalAlignment="Center" FontSize="12" Margin="-1,-2,0,0" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            </StackPanel>
        </Grid>

        <Grid Grid.Row="1" Padding="25,0,25,20">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto" />
                <RowDefinition Height="*" />
            </Grid.RowDefinitions>
            <Grid.Resources>
                <local:BoolToOpacityConverter x:Key="BoolToOpacityConverter" EnabledOpacity="1" DisabledOpacity="0.4" />
            </Grid.Resources>

            <CommandBar Grid.Row="0" DefaultLabelPosition="Right" BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" BorderThickness="1" Margin="0,16,0,12">
                <AppBarButton x:Uid="AppBarAdd" Icon="Add" Click="OnAddFiles" />
                <AppBarButton x:Uid="AppBarFolder" Icon="Folder" Click="OnAddFolder" />
                <AppBarButton x:Uid="AppBarClear" Icon="Clear" Click="OnClearAll" />
                <AppBarSeparator />
                <AppBarButton x:Uid="AppBarSaveConfig" Icon="Save" Click="OnSaveConfig" />
                <AppBarButton x:Uid="AppBarLoadConfig" Icon="OpenFile" Click="OnLoadConfig" />
                <AppBarSeparator />
                <AppBarButton x:Uid="AppBarUndo" Icon="Undo" Click="OnUndo" />
                <AppBarElementContainer VerticalAlignment="Center">
                    <Button Style="{StaticResource AccentButtonStyle}" Click="OnApply" Padding="10,6" VerticalAlignment="Center" IsEnabled="{x:Bind CanApply, Mode=OneWay}">
                        <StackPanel Orientation="Horizontal" Spacing="8" VerticalAlignment="Center">
                            <FontIcon Glyph="" FontSize="16" FontFamily="Segoe MDL2 Assets" />
                            <TextBlock x:Uid="ApplyButtonText" VerticalAlignment="Center" />
                        </StackPanel>
                    </Button>
                </AppBarElementContainer>
            </CommandBar>

            <Grid Grid.Row="1" ColumnSpacing="12" RowSpacing="8">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="3*" />
                </Grid.ColumnDefinitions>

                <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" VerticalAlignment="Stretch">
                    <StackPanel Spacing="10">
                        <Border Padding="12" CornerRadius="12" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" BorderThickness="1">
                            <StackPanel Spacing="10">
                                <!--<TextBlock Text="Règles" Style="{StaticResource SubtitleTextBlockStyle}" />-->

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="ScopeLabel" />
                                    <ComboBox x:Name="ScopeBox" SelectionChanged="OnRuleChanged" SelectedIndex="0" HorizontalAlignment="Stretch">
                                        <ComboBoxItem x:Uid="ScopeNameItem" Tag="Name" />
                                        <ComboBoxItem x:Uid="ScopeExtensionItem" Tag="Extension" />
                                        <ComboBoxItem x:Uid="ScopeFullItem" Tag="Full" />
                                    </ComboBox>
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="PrefixLabel" />
                                    <TextBox x:Name="PrefixBox" x:Uid="PrefixBox" TextChanged="OnRuleChanged" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="SuffixLabel" />
                                    <TextBox x:Name="SuffixBox" x:Uid="SuffixBox" TextChanged="OnRuleChanged" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="SearchLabel" />
                                    <TextBox x:Name="SearchBox" x:Uid="SearchBox" TextChanged="OnRuleChanged" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="ReplaceLabel" />
                                    <TextBox x:Name="ReplaceBox" x:Uid="ReplaceBox" TextChanged="OnRuleChanged" />
                                </StackPanel>

                                <StackPanel Spacing="4">
                                    <CheckBox x:Name="RegexToggle" x:Uid="RegexToggle" Checked="OnRegexOptionChanged" Unchecked="OnRegexOptionChanged" />
                                    <CheckBox x:Name="RegexAllToggle" x:Uid="RegexAllToggle" IsEnabled="{Binding IsChecked, ElementName=RegexToggle}" Checked="OnRegexOptionChanged" Unchecked="OnRegexOptionChanged" />
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <Grid ColumnSpacing="8" RowSpacing="4" Margin="0">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="140" />
                                            <ColumnDefinition Width="140" />
                                        </Grid.ColumnDefinitions>
                                        <Grid.RowDefinitions>
                                            <RowDefinition Height="Auto" />
                                            <RowDefinition Height="Auto" />
                                        </Grid.RowDefinitions>
                                        <TextBlock x:Uid="InsertLabel" Margin="0" />
                                        <TextBlock Grid.Row="0" Grid.Column="0" Text="" />
                                        <TextBlock Grid.Row="0" Grid.Column="1" x:Uid="InsertAfterLabel" />

                                        <TextBox Grid.Row="1" Grid.Column="0" x:Name="InsertTextBox" x:Uid="InsertTextBox" Width="140" Height="32" TextChanged="OnRuleChanged" />
                                        <NumberBox Grid.Row="1" Grid.Column="1" x:Name="InsertPositionBox" Minimum="0" Value="0" Height="32" ValueChanged="OnInsertRuleChanged" />
                                    </Grid>
                                </StackPanel>

                                <StackPanel Spacing="6">
                                    <TextBlock x:Uid="CaseLabel" />
                                    <ComboBox x:Name="CaseModeBox" SelectionChanged="OnCaseChanged">
                                        <ComboBoxItem x:Uid="CaseNoneItem" Tag="None" IsSelected="True" />
                                        <ComboBoxItem x:Uid="CaseLowerItem" Tag="Lower" />
                                        <ComboBoxItem x:Uid="CaseUpperItem" Tag="Upper" />
                                        <ComboBoxItem x:Uid="CaseTitleItem" Tag="Title" />
                                    </ComboBox>
                                    <CheckBox x:Name="RemoveAccentsToggle" x:Uid="RemoveAccentsToggle" Checked="OnRuleChanged" Unchecked="OnRuleChanged" />
                                </StackPanel>

                                <Expander x:Uid="RemoveCharsExpander" IsExpanded="False" HorizontalAlignment="Stretch" Margin="0,0">
                                    <StackPanel Spacing="6" Margin="8,6,8,0">
                                        <StackPanel Orientation="Horizontal" Spacing="12">
                                            <StackPanel Width="130">
                                                <CheckBox x:Name="RemoveFirstToggle" x:Uid="RemoveFirstToggle" Checked="OnRuleChanged" Unchecked="OnRuleChanged" />
                                                <NumberBox x:Name="RemoveFirstBox" Minimum="0" Value="0" Height="32" ValueChanged="OnNumberRuleChanged" IsEnabled="{Binding IsChecked, ElementName=RemoveFirstToggle}" />
                                            </StackPanel>
                                            <StackPanel Width="130">
                                                <CheckBox x:Name="RemoveLastToggle" x:Uid="RemoveLastToggle" Checked="OnRuleChanged" Unchecked="OnRuleChanged" />
                                                <NumberBox x:Name="RemoveLastBox" Minimum="0" Value="0" Height="32" ValueChanged="OnNumberRuleChanged" IsEnabled="{Binding IsChecked, ElementName=RemoveLastToggle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Expander>

                                <Expander x:Uid="NumberingExpander" IsExpanded="False" HorizontalAlignment="Stretch" Margin="0,0">
                                    <StackPanel Spacing="6" Margin="8,6,8,0">
                                        <CheckBox x:Name="NumberingToggle" x:Uid="NumberingToggle" Checked="OnRuleChanged" Unchecked="OnRuleChanged" />

                                        <StackPanel Spacing="8">
                                            <StackPanel Orientation="Horizontal" Spacing="8">
                                                <StackPanel Width="130">
                                                    <TextBlock x:Uid="NumberStartLabel" Opacity="{Binding IsChecked, ElementName=NumberingToggle, Converter={StaticResource BoolToOpacityConverter}}" />
                                                    <NumberBox x:Name="NumberStartBox" Minimum="0" Value="1" ValueChanged="OnNumberRuleChanged" IsEnabled="{Binding IsChecked, ElementName=NumberingToggle}" />
                                                </StackPanel>
                                                <StackPanel Width="130">
                                                    <TextBlock x:Uid="NumberPaddingLabel" Opacity="{Binding IsChecked, ElementName=NumberingToggle, Converter={StaticResource BoolToOpacityConverter}}" />
                                                    <NumberBox x:Name="NumberPaddingBox" Minimum="1" Maximum="8" Value="3" ValueChanged="OnNumberRuleChanged" IsEnabled="{Binding IsChecked, ElementName=NumberingToggle}" />
                                                </StackPanel>
                                            </StackPanel>
                                            <StackPanel Width="130">
                                                <TextBlock x:Uid="NumberSeparatorLabel" Opacity="{Binding IsChecked, ElementName=NumberingToggle, Converter={StaticResource BoolToOpacityConverter}}" />
                                                <TextBox x:Name="NumberSeparatorBox" x:Uid="NumberSeparatorBox" TextChanged="OnRuleChanged" IsEnabled="{Binding IsChecked, ElementName=NumberingToggle}" />
                                            </StackPanel>
                                        </StackPanel>
                                    </StackPanel>
                                </Expander>
                            </StackPanel>
                        </Border>
                    </StackPanel>
                </ScrollViewer>

                <Border Grid.Column="1" Padding="12" CornerRadius="12" Background="{ThemeResource LayerOnMicaBaseAltFillColorDefaultBrush}" BorderBrush="{ThemeResource ControlStrokeColorSecondaryBrush}" BorderThickness="1">
                    <Grid RowSpacing="8">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="*" />
                        </Grid.RowDefinitions>
                        <InfoBar x:Name="StatusBar" IsOpen="False" Severity="Informational" />

                        <StackPanel Grid.Row="1" Orientation="Horizontal" Spacing="8" HorizontalAlignment="Right">
                            <ComboBox x:Name="FilterBox" SelectionChanged="OnFilterChanged" MinWidth="160" SelectedIndex="0" Height="32">
                                <ComboBoxItem x:Uid="FilterAll" Tag="All" />
                                <ComboBoxItem x:Uid="FilterEnabledOnly" Tag="EnabledOnly" />
                                <ComboBoxItem x:Uid="FilterDisabledOnly" Tag="DisabledOnly" />
                                <ComboBoxItem x:Uid="FilterWithChanges" Tag="WithChanges" />
                                <ComboBoxItem x:Uid="FilterWithoutChanges" Tag="WithoutChanges" />
                            </ComboBox>
                        </StackPanel>
                        
                        <Grid Grid.Row="2" AllowDrop="True" DragOver="OnFilesDragOver" Drop="OnFilesDrop">
                            <ListView x:Name="FilesList" SelectionMode="Extended">
                                <ListView.ContextFlyout>
                                    <MenuFlyout>
                                        <MenuFlyoutItem x:Uid="MenuRemoveSelected" Icon="Delete" Click="OnRemoveSelected" />
                                    </MenuFlyout>
                                </ListView.ContextFlyout>
                                <ListView.ItemContainerStyle>
                                    <Style TargetType="ListViewItem">
                                        <Setter Property="Padding" Value="0" />
                                        <Setter Property="Margin" Value="0" />
                                        <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                                    </Style>
                                </ListView.ItemContainerStyle>
                                <ListView.Header>
                                    <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,1" Padding="0,6">
                                        <Grid Padding="8,6" ColumnSpacing="8">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="40" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="2*" />
                                            <ColumnDefinition Width="{x:Bind DirectoryColumnWidth, Mode=OneWay}" />
                                        </Grid.ColumnDefinitions>
                                            <Border Grid.Column="0" Width="40" />
                                            <TextBlock Text="{x:Bind NomHeaderText, Mode=OneWay}" Grid.Column="1" FontWeight="SemiBold" />
                                            <TextBlock Text="{x:Bind ApercuHeaderText, Mode=OneWay}" Grid.Column="2" FontWeight="SemiBold" />
                                            <TextBlock x:Uid="DirectoryHeader" Grid.Column="3" FontWeight="SemiBold" Visibility="{x:Bind DirectoryColumnVisibility, Mode=OneWay}" />
                                        </Grid>
                                    </Border>
                                </ListView.Header>
                                <ListView.ItemTemplate>
                                    <DataTemplate x:DataType="local:FileEntry">
                                        <Border BorderBrush="{ThemeResource ControlStrokeColorDefaultBrush}" BorderThickness="0,0,0,1">
                                            <Grid Padding="8,4" MinHeight="44" ColumnSpacing="8" Opacity="{Binding IsEnabled, Converter={StaticResource BoolToOpacityConverter}}">
                                                <Grid.ColumnDefinitions>
                                                    <ColumnDefinition Width="40" />
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="2*" />
                                                    <ColumnDefinition Width="{x:Bind DirectoryColumnWidth, Mode=OneWay}" />
                                                </Grid.ColumnDefinitions>
                                                <CheckBox Grid.Column="0" IsChecked="{x:Bind IsEnabled, Mode=TwoWay}" Checked="OnFileEnabledToggled" Unchecked="OnFileEnabledToggled" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="1" Text="{x:Bind OriginalName, Mode=OneWay}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="2" Text="{x:Bind PreviewName, Mode=OneWay}" Foreground="{x:Bind PreviewForeground, Mode=OneWay}" VerticalAlignment="Center" />
                                                <TextBlock Grid.Column="3" Text="{x:Bind DisplayDirectory}" Opacity="0.7" TextTrimming="CharacterEllipsis" VerticalAlignment="Center" Visibility="{x:Bind DirectoryColumnVisibility, Mode=OneWay}" />
                                            </Grid>
                                        </Border>
                                    </DataTemplate>
                                </ListView.ItemTemplate>
                            </ListView>
                            <StackPanel x:Name="EmptyState" HorizontalAlignment="Center" VerticalAlignment="Center" Opacity="0.6" Visibility="Collapsed" IsHitTestVisible="False" Spacing="8">
                                <SymbolIcon Symbol="Upload" Width="36" Height="36" />
                                <TextBlock x:Uid="EmptyStateText" TextAlignment="Center" />
                            </StackPanel>
                        </Grid>
                    </Grid>
                </Border>
            </Grid>
        </Grid>
    </Grid>
</Window>
