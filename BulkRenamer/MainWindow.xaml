<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="BulkRenamer.MainWindow"
    x:Name="RootWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:BulkRenamer"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    mc:Ignorable="d"
    Title="Batch Renaming">

    <Window.SystemBackdrop>
        <MicaBackdrop />
    </Window.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="32" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid x:Name="AppTitleBarHost" Height="32" Padding="8,0" VerticalAlignment="Top" HorizontalAlignment="Stretch" Background="Transparent">
            <StackPanel Orientation="Horizontal" Spacing="6" VerticalAlignment="Center">
                <Image Source="ms-appx:///Assets/Square44x44Logo.targetsize-24_altform-unplated.png" Width="17" Height="17" Stretch="Uniform" />
                <TextBlock x:Uid="AppTitle" VerticalAlignment="Center" FontSize="12" Margin="1,-2,0,0" Foreground="{ThemeResource TextFillColorPrimaryBrush}" />
            </StackPanel>
        </Grid>

        <NavigationView Grid.Row="1" x:Name="NavView" PaneDisplayMode="LeftCompact" IsSettingsVisible="True" SelectionChanged="OnNavSelectionChanged" IsPaneOpen="False" IsBackButtonVisible="Collapsed" BackRequested="OnNavBackRequested">
            <NavigationView.MenuItems>
                <NavigationViewItem x:Uid="NavRenamingItem" Icon="Home" Content="Renaming" Tag="Home" IsSelected="True" />
                <NavigationViewItem x:Uid="NavHistoryItem" Icon="Clock" Content="History" Tag="History" />
            </NavigationView.MenuItems>
            <NavigationView.Content>
                <Frame x:Name="ContentFrame">
                    <Frame.ContentTransitions>
                        <TransitionCollection>
                            <NavigationThemeTransition />
                        </TransitionCollection>
                    </Frame.ContentTransitions>
                </Frame>
            </NavigationView.Content>
        </NavigationView>
    </Grid>
</Window>
